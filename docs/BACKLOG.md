# 백로그 — 아직 매핑하지 못한 리소스

실기기가 보고하는데 capability로 연결하지 못한 리소스들입니다. 대부분 **필드명은 보이지만
의미나 쓰기 계약을 모르는** 경우입니다.

이 목록의 항목은 추측으로 채우지 않습니다. 주방 후드가 그렇게 이식됐다가 필드명이 전부
틀려서 페어링은 되고 아무것도 제어되지 않았고, 테스트는 전부 통과했습니다. 냉장고의 `253`도
단일 덤프만 봤을 때는 켈빈 인코딩으로 보였지만, 같은 모델을 다른 모드로 쓰는 두 번째 유닛이
반증했습니다.

**확정 방법은 대조 실험입니다.** 기기에서 설정을 하나 바꾸고 다시 덤프해 무엇이 변했는지
보면 됩니다.

```sh
homey api raw --path "/api/app/com.lomohome.localthings/resources?host=<IP>&raw=1"
```

`raw=1` 없이 호출하면 시리얼·MAC·Wi-Fi 이름이 가려집니다. 공유할 때는 `raw=1`을 빼세요.

쓰기가 실제로 커밋되는지는 되읽기까지 해주는 진단 엔드포인트로 확인합니다. 냉장고 설정값이
두 번 잘못 구현된 뒤에야 이걸 만들었는데, 그 즉시 답이 나왔습니다 — 추측하지 말고 이걸 쓰세요.

```sh
homey api raw -X POST \
  --path /api/app/com.lomohome.localthings/write-resource \
  --body '{"host":"192.168.1.203","path":"/temperature/desired/cooler/0",
           "body":{"temperature":4}}'
```

응답에 가전이 돌려준 값(`response`), 수락 여부(`accepted`), 그리고 **쓰기 직후 다시 읽은
리소스**(`after`)가 담깁니다. `accepted`가 true인데 `after`가 안 바뀌면 "받아들이지만
반영하지 않는" 경로입니다 — 냉장고 벤더 경로가 정확히 그랬습니다.

---

## 냉장고 (`TP2X_REF_21K`, 키친핏)

### 설정 온도 — 재현되지 않았습니다 (2026-08-03 실측)

사용자 보고를 받아 세 대 모두 **UI가 쓰는 것과 같은 경로**(capability PUT)로 시험했고, 전부
정상 동작했습니다. 라이브 읽기로 확인했으며 캐시를 쓰지 않았습니다.

| 기기 | 시험 | 결과 |
|---|---|---|
| 냉장고 | 2 → 3 → 2 ℃ | OCF·벤더 리소스 양쪽에 반영 |
| 냉동고 | −20 → −19 → −20 ℃ | OCF·벤더 리소스 양쪽에 반영 |

그래서 **코드를 고치지 않았습니다.** 재현되지 않는 것을 추측으로 손대면 동작하는 것을 깨뜨릴
위험만 있습니다. 다시 발생하면 다음을 좁혀야 합니다 — 어느 기기였는지, 기기 화면인지
플로우인지, 어떤 값이었는지(범위 밖 값은 쓰기가 거부됩니다), 그리고 바꾼 직후인지 한참 뒤인지
(`WRITE_SETTLE_S`는 4초입니다).

참고로 레퍼런스는 이 대비를 명시적으로 적어 둡니다 — "일부 모델은 벤더 `/temperatures/vs/0`
경로만 변경을 반영한다. 존재할 때 벤더 경로를 쓰는 것이 항상 옳다." 우리 세 대는 OCF 경로로도
반영되므로 지금 바꿀 이유가 없지만, **미검증 기종에서 이 증상이 보고되면 여기부터 봐야
합니다.** item ID 규약은 `"0"` = 냉동, `"1"` = 냉장/쿨러입니다.

### 두 경로가 존재한다는 사실은 그대로 유효합니다

| 경로 | href | writer | `exists` 게이트 |
|---|---|---|---|
| OCF 칸별 | `_ocf_desired("cooler"/"freezer")` | `_write_ocf_setpoint` | `_ocf_active` |
| 벤더 집계 | `/temperatures/vs/0` | `_write_fridge_setpoint` | `_compartment_active(...)` |

게이트로 갈라 하나만 바인딩합니다. 우리 세 대는 OCF 쪽이 바인딩되고 그것이 반영됩니다.
0.4.1 체인지로그가 이 문제를 한 번 고쳤다고 적고 있으니, 벤더 경로 writer는 **그때 이후 실사용
경로가 아닙니다** — 나중에 벤더 우선으로 바꾼다면 그 writer부터 실측해야 합니다.

리스트형 리소스에 쓸 때는 `_fold_write_into`를 함께 볼 것. `dict.update`로 접으면 `items`
전체가 교체되어 `current`/`min`/`max`가 비는 버그가 있었습니다.

### 변온고 모드 변경 — 가전이 원격 변경을 받지 않습니다 (종결, 1.0.2)

**결론: 구현할 수 없습니다.** 소유자가 확인한 결정적 사실 — **삼성 공식 앱에서도 변온실
냉장↔냉동 전환이 되지 않습니다.** 우리 구현의 문제가 아니라 이 기기의 성질입니다.

경과를 남깁니다. 1.0.1에서 capability를 `enum`/쓰기가능으로 바꾸고 writer를 넣었고, 1.0.2에서
되돌렸습니다. **페이로드는 문제가 아니었습니다** — 두 대에 각자의 현재 모드를 그대로 다시 쓰는
검증에서 리소스가 경로와 페이로드를 수락하고 `x.com.samsung.da.modes`의 다른 플래그도 온전히
유지됐습니다. 즉 쓰기는 받아들여지지만 **가전이 그것으로 칸을 전환하지 않습니다.** 승인 응답이
증거가 아니라는 이 저장소의 원칙이 또 한 번 맞았습니다.

동작하지 않는 제어를 내보내는 것이 아무것도 없는 것보다 나쁩니다. capability는 읽기 전용
문자열로 돌아갔고, 미지 토큰은 다시 그대로 표시합니다(김치가 실재하므로).

**레퍼런스에서 다시 가져오지 마세요.** 레퍼런스의 flex-zone capability에는 쓰기가 있지만 그건
다른 기종군입니다 — 우리 두 대의 `/mode/vs/0` 라이브 응답에는 `supportedOptions` 필드가 아예
없어서 레퍼런스의 탐색 방식은 여기서 아무것도 바인딩하지 못합니다. 새 증거 없이 되살리면
1.0.1을 반복하게 됩니다.

**다시 열 조건**: 이 전환을 실제로 받아주는 기기가 나타나는 것. 그때는 위의 페이로드를 그대로
쓰면 됩니다.

### 냉장전용 냉장고의 문열림이 간헐적으로 어긋납니다 (미해결, 재현 실패)

**증상** (소유자 보고 2026-08-03): 냉장고(냉장전용)의 문열림이 "아예 안 되는 건 아닌데 이상하게
왔다갔다" 합니다. 변온고 두 대는 잘 읽힙니다.

**재현하지 못했습니다.** 문을 열고 닫는 동안 실측했고 두 전환 모두 정확했습니다.

```
열림  → /doors/vs/0 = Open,  /door/onedoorfreezer/vs/0 = Open,  alarm_contact = true
닫힘  → 둘 다 Close,                                            alarm_contact = false  (3회 샘플)
```

**배제된 가설 두 개.** 다시 세우지 마세요.

1. **"집계 배열이 일부만 전달돼 OR이 흔들린다"** — 아닙니다. 세 대 모두 `/doors/vs/0`의
   `items[]`에 항목이 **하나뿐**입니다(`id "3"`, `description "Door"`). 부분 전달로 갈릴 배열이
   없습니다.
2. **"`/door/onedoorfreezer/vs/0`에 바인딩된 spec이 없어 그 알림이 재계산을 못 시킨다"** —
   아닙니다. `exists` 게이트는 그 spec을 어느 기기에서도 통과시키지 않지만, `_apply`는
   `applies()`를 **보지 않고** capability가 기기에 있는지만 확인합니다. 그래서 두 href의 알림이
   모두 `alarm_contact`을 재계산시킵니다. 게이트는 `_sync_capabilities`에만 영향을 줍니다.

**남은 설명은 별도 버그가 아닙니다.** 양쪽 알림이 함께 유실되면 값이 어긋나고, 냉장고는
`push=true`이므로 다음 **300초 sweep**까지 그대로 남습니다. 냉장고에서만 눈에 띄는 이유도 이걸로
설명됩니다 — 냉장고 문은 하루에 수십 번 열리고 변온고는 거의 안 열리므로, 같은 유실률에서도 자주
변하는 기기에서만 보입니다. 즉 위쪽 "초기 알림 유실" 항목의 증상입니다.

**다음에 증상을 보면 할 일**: 문 열림·닫힘을 타임라인에 기록해(운전모드·설정온도가 이미 쓰는
기제) 시각과 함께 남기는 것. 알림 유실이면 누락이나 최대 5분 지연이 그대로 보입니다. 소유자와
합의해 증상이 다시 나타날 때까지 미뤘습니다 — 재현하지 못한 것을 고치면 잘 되는 것을 깨뜨릴 수
있습니다.

### 김치 냉각 강도 — `/mode/vs/0`

```
x.com.samsung.da.supportedOptions = ["CV_TMF_KIMCHI_WEAKCOOL",
                                     "CV_TMF_KIMCHI_NORMAL",
                                     "CV_TMF_KIMCHI_STRONGCOOL"]
```

변온고 두 대 모두 이 세 값을 광고하지만, **현재 어느 것이 선택돼 있는지 알려주는 필드가
없습니다.** `modes`에는 `MULTIROOM_COOLER`/`MULTIROOM_FREEZER`만 있고 `CV_TMF_*`는 없습니다.

레퍼런스는 이 상태를 `/status/kimchi/<slot>/vs/0`의 `currentMode`/`supportMode`에서 읽는데,
**이 기기에는 그 리소스가 없습니다.**

- **실험**: 변온실을 **김치 모드로 전환**한 뒤 덤프. `modes`의 세 번째 토큰이
  `MULTIROOM_KIMCHI` 같은 값으로 바뀌는지, 그리고 `CV_TMF_*` 중 하나가 어딘가에 나타나는지
  확인. 그다음 강도를 약냉→강냉으로 바꿔 어느 필드가 따라 움직이는지 봅니다.
- 알아내면: 변온실 모드에 김치 값을 추가하고, 강도를 별도 enum(읽기 전용부터)으로 노출.

### 자동문 타이머 — `/autodoor/timer/vs/0`

```
x.com.samsung.da.time.desired          = "2"
x.com.samsung.da.time.supportedOptions = ["2", "3", "4"]
```

세 대 모두 있습니다. **단위를 모릅니다** — 2/3/4초일 수도, 분일 수도, 단계 이름일 수도
있습니다. 레퍼런스에 이 리소스는 아예 없습니다.

- **실험**: SmartThings 앱에서 이 설정의 UI 라벨을 확인(초/분 표기), 또는 값을 4로 바꾸고
  덤프해 필드가 따라오는지 확인.
- 알아내면: 단위가 확인될 때만 노출. 단위 없는 숫자는 사용자에게 의미가 없습니다.

### `ado.soundcontrol` — `/status/lock/vs/0`

```
x.com.samsung.da.ado.soundcontrol = "On"
```

레퍼런스는 같은 href에서 `ado.devicecontrol`(자동문 개폐)과 `device.sound`(기기 알림음)를
읽습니다. **이건 그 둘과 다른 세 번째 필드명**이라 어느 쪽인지, 아니면 "자동문 작동음"이라는
제3의 설정인지 불명확합니다. 이름만으로 스위치를 만들면 사용자가 끄려던 것과 다른 것이 꺼질
수 있습니다.

- **실험**: SmartThings 앱에서 알림음 관련 설정을 끄고 덤프해 이 필드가 `Off`가 되는지 확인.
- 알아내면: 쓰기 가능 스위치(레퍼런스의 `_status_lock_write` 계약과 동일한 형태).

### `/energy/ailevel/vs/0`

일반 냉장고(변온 없는 변형)에만 있습니다. 내용 확인 필요.

### `/drlc/0`, `/drlc/vs/0`

수요반응(Demand Response) 제어. 냉장고·후드·에어컨에 모두 있습니다.
`drlcLevel`, `durationminutes`, `start`, `override`, `realSaving`. 한국 전력 환경에서 실제로
쓰이는지 불명. 진단용 읽기 전용으로는 넣을 수 있습니다.

---

## 에어컨 (`TP1X_DA-AC-CAC-01001`)

### `DA_SAC_M_*` — 에러가 아닙니다 (2026-08-07, 의미는 미확정)

사용자가 `DA_SAC_M_0003`을 **에러로 신고**했는데 스마트싱스는 정상이라고 했습니다.
스마트싱스가 맞습니다.

`/alarms/vs/0`은 이 보드에서 **독립된 슬롯 3개**입니다.

| id | 계열 | 우리 4대의 값 |
|---|---|---|
| 0 | `ErrorCode_*` — **에러 채널** | 다섯 번의 관측 내내 4대 모두 `ErrorCode_OFF` / `Deleted` |
| 1 | `FilterAlarm*` | 유닛별로 다름 |
| 2 | `DA_SAC_M_*` | `_OFF` 또는 `0003`/`0004` |

에러가 아니라는 근거 셋:

- **에러 슬롯이 계속 "이상 없음"입니다.** 스마트싱스가 정상이라 하는 이유가 이것입니다.
- **값이 스스로 변합니다.** 손님방 `0003`→`0004`, 예린방도 나중에 같은 이동. 고장 코드는
  그렇게 움직이지 않습니다.
- **공개 문서에 존재하지 않습니다.** 깃헙 전체에서 이 계열의 히트는 레퍼런스 CAC 픽스처의
  `DA_SAC_M_OFF` 한 줄, 웹은 0건. 삼성 소비자 코드(E1·CF·dF…)와 형식이 다릅니다.

**반증된 가설 셋** — 다시 시도하지 마십시오, 전부 실측으로 죽었습니다:

| 가설 | 반증 |
|---|---|
| 음소거 상태 | 4대 전부 `mute`/볼륨 0인데 한 대만 `_OFF` |
| 전원 On | 그 한 대를 켰는데도 `_OFF` 유지 |
| 무풍(`Nano`) 설정 | 그 대도 원래 `Nano`였음 — 쓰기가 `errorCode: "unchanged"`로 거부됐고 슬롯 불변. 꺼져 있을 때 `convenient_mode`가 `Off`로 읽히는 것에 속았습니다 |

**확정된 것 — 이 슬롯은 "운전 중인가"입니다** (2026-08-07/08, 12분 간격 8시간, 4대,
유닛-샘플 152건, `dasacm-monitor.log`):

| 코드 | `onoff` | 예외 |
|---|---|---|
| `_OFF` | False **110/110** | 0 |
| 숫자 | True **58/58** | 0 |

**설정온도 도달은 조건이 아닙니다.** 이 문서의 이전 판이 그렇게 적었는데 로그가 반증했습니다
— 실온이 설정온도를 **초과한 상태에서 숫자가 붙은 샘플이 12건**입니다(`0003`에 5건,
`0004`에 7건). 그 서술도 한 시점 관측에서 나왔고, 이 슬롯에 같은 방식으로 당한 것이 이번이
**네 번째**입니다.

**미해결 하나**: 켠 지 약 2분 30초 뒤에 읽으면 `_OFF`였습니다(두 번 모두). 로그에는 전원을
켜는 전이가 한 건도 없어서, 슬롯이 단순히 늦게 따라오는 것인지 확인되지 않았습니다.
**확정 방법**: 한 대를 켜고 5·10·15분 뒤에 읽어 언제 숫자로 바뀌는지 봅니다.

**숫자 3 vs 4는 미해결**입니다. 유닛별로 느리게 움직입니다 — 안방은 8시간 내내 `0003`,
손님방은 `0004`→`0003`→`0004`. 로그에 있는 어떤 필드로도(운전 모드, 풍량, 설정온도 차,
소비전력) 갈리지 않습니다. CAC는 실외기를 공유하는 시스템 에어컨일 수 있어 실외기 단위의
값일 가능성이 남아 있지만, **근거는 없습니다.**

`localthings_operation_state_code`를 **Insights 켜서** 숫자로 노출했습니다(`_OFF`=0) —
`measure_power`·`measure_temperature`와 같은 그래프에 겹쳐 보기 위해서입니다. 이름을
중립적으로 둔 것도 의도입니다. "압축기"라고 붙이면 미확정 가설을 제품에 새기는 셈입니다.

### 알람 표시의 결함 2종 (미수정)

위 신고가 드러낸 것입니다. **정체와 무관하게 성립합니다.**

1. **`_OFF` 센티널을 알람으로 표시합니다.** `_ALARM_IDLE_CODES`가
   `{errorcode_off, ct_e_off, none, ""}` 하드코딩이라 나머지 `_OFF` 형태를 모릅니다. 우리
   가전은 `_OFF` 항목에 `state: Deleted`를 붙여줘 우연히 걸러졌을 뿐이고, **state를 아예
   안 보내는 보드**가 있습니다. 레퍼런스 덤프로 우리 리더를 돌리면 **16건이 가짜 알람**을
   냅니다 — 오븐·레인지·전자레인지는 **전 덤프**가 `OV_E_OFF`를, 에어컨 일부가
   `AC_V_0002_OFF`·`FilterAlarm_OFF`를 알람으로 띄웁니다. `localthings_alarm_code`는 18개
   레지스트리 전부에 붙어 있습니다. **수정 방향**: `_OFF`로 끝나는 코드는 계열 불문 유휴로
   처리. 6개 계열에서 관측된 규칙이고, 그 16건이 그대로 회귀 테스트가 됩니다.
2. **무관한 슬롯끼리 서로를 가립니다.** 첫 활성 항목만 표시하므로 손님방만(필터 알람이
   해제돼 있어) 이 값이 드러났습니다. 표시값이 다른 슬롯 상태에 좌우됩니다.

### `triggeredTime`은 발생 시각이 아닙니다 (2026-08-07)

`/alarms/vs/0`의 모든 항목이 **매 읽기마다** 이 필드를 갱신합니다 — 값이 안 바뀐 항목까지
전부. 13:42:4x에 읽으면 13:42:4x, 5분 뒤 읽으면 13:47:3x입니다. **이 필드로는 아무것도
날짜를 매길 수 없습니다.** 알람이 언제 발생했는지 알아내려면 폴링 기록이 필요합니다.

### 모션 센서 — 라이브 재실 값은 없습니다 (조사 완료)

`MDS`는 삼성이 쓰는 Motion Detection Sensor의 약자이고, 관련 리소스가 네 개 있습니다.
이 중 셋은 **설정**으로 확정해 바인딩했습니다(부재 감지 on/off, 부재 판정 분,
모션 모니터링 on/off — 모두 읽기 전용).

**재실 여부를 알려주는 값은 이 펌웨어에 없습니다.** 결론이지 추측이 아니며, 근거는 두
가지입니다.

첫째, `/mds/absencestate/vs/0`의 `status`/`absenceTime`은 재실과 상관이 없습니다. 실제
상황을 알려받아 대조한 결과:

| 방 | 실제 | `status` | `absenceTime` |
|---|---|---|---|
| 안방 | 사람 있음 | Off | 0 |
| 예린방 | 사람 있음(취침) | On | 30 |
| 손님방 | **비어 있음** | Off | 0 |

빈 방과 사람 있는 방이 같은 값입니다. `supportedTimes`가 `["0","30","60","120"]`으로
`absenceTime`의 선택지 모양이고 두 값이 짝지어 움직이므로, "N분간 움직임이 없으면 부재로
판정" 임계값 설정으로 봅니다.

둘째, 레퍼런스가 언급하는 `motionState`(+`supportedMotionState`) 필드를 네 대 전부에서
검색했지만 **어디에도 없습니다.**

### `maxDetectCount` — 로그가 아니라 학습된 패턴으로 보입니다

`/mds/absencestate/vs/0`의 48칸 배열(값 0~3). 30분 × 48 = 24시간이고, 방별 생활 패턴과 잘
맞습니다(예린방은 07:00~21:30이 3, 심야는 0).

**35분 간격으로 두 번 떠서 비교했더니 네 대 모두 한 칸도 바뀌지 않았습니다.** 그 사이 안방에는
사람이 있었습니다. 즉 라이브 카운터가 아닙니다. 이름이 `maxDetectCount`("최대" 감지 수)인
점과 합치면, 가전이 부재를 예측하는 데 쓰는 **누적·학습된 시간대별 프로필**로 보입니다.

- **한계**: 관측 창 동안 에어컨 네 대가 모두 꺼져 있었습니다. "운전 중에만 갱신된다"는
  가능성은 배제하지 못했습니다. 확인하려면 에어컨을 켜 둔 채 같은 비교를 반복해야 합니다.
- 갱신 주기가 확인되지 않는 한 타일에 올릴 값이 아닙니다. 라이브가 아닌 값을 모션 센서처럼
  보여주면 빈 방을 "감지됨"으로 표시하게 됩니다.

### AI 절전 — `/aisleep/vs/0`

```
displayNightMode, elapsedTime, requestFeedback, resultFeedback,
statusFeedback, sleepTime = "14002200"
```

`sleepTime`이 `HHMM`+`HHMM`(14:00–22:00) 형태로 보이지만 확인 안 됨. `test_registry.py`가
이 href를 미매핑 목록에 남겨 두어 잊히지 않게 하고 있습니다.

---

## 후드 (`AHD-WW-TP1-22`)

### `/airlevelcheck/vs/0`

```
periodicSensingActivationState, periodicSensingInterval = "3600",
sensingState, lastSensingTime, lastSensingLevel = "Kr1",
autoExeState = "Sensing", supportedAutoExeState = ["Airpurify", "Alarm"]
```

주기적 공기질 감지. `lastSensingLevel`의 `Kr1`은 한국 기준 등급으로 보이나 척도 불명.
`autoExeState`는 쓰기 가능해 보이지만(`supportedAutoExeState`가 있음) 미확인.

### `/autoventilation/vs/0`

```
action = "Start"
```

레퍼런스도 이 필드를 그냥 센서로만 노출합니다. 명령용인지 상태용인지 불명.

---

## 인덕션 (`TP1X_DA-KS-COOKTOP-01011`, NV9000D-/KO4)

### 이 인덕션은 쓰기를 전혀 받지 않습니다 (종결, 1.0.3에서 철회)

**결론: 이 가전에는 쓰기 가능한 제어가 없습니다.** 소유자가 인덕션 앞에 있는 상태로
2026-08-04에 실측했습니다.

| 요청 | 결과 |
|---|---|
| `POST /cooktop/status/vs/0` `{"power":"off"}` — 꺼짐 | **4.05 Method Not Allowed** |
| `POST /cooktop/status/vs/0` `{"power":"off"}` — 화구 동작 중 | **4.05** |
| `POST /cooktop/status/vs/0` `{"childLock":"on"}` — 화구 동작 중 | **4.05** |
| 앱 UI의 어린이 보호 토글 (실제 경로) — 화구 동작 중 | 오류 |
| `GET /cooktop/status/vs/0` | 정상, `smartControlState: on` |
| **대조군**: `POST /hood/lamp/vs/0` (같은 시각, 후드) | `accepted: true` |

대조군이 결론을 한 걸음에 만들었습니다 — 우리 전송 계층은 정상이고, 거부는 이 가전의
정책입니다. PUT은 쓸 수 없습니다(`smartthings_local`의 세션이 GET·POST만 구현합니다).

**틀린 추론의 경과.** `/cooktop/spec/vs/0`은 여전히
`supportedFeatureList = ["kitchenService", "remoteChildLock", "remotePowerOff"]`를 광고합니다.
1.0.3은 여기서 "`remotePowerOff`가 있으니 끄기는 쓸 수 있다"고 읽었고, 그 신뢰의 근거는
"`remoteChildLock`도 목록에 있고 차일드락 쓰기는 이 기기에서 검증된 유일한 쓰기"라는 코드
주석이었습니다. **두 절반이 모두 틀렸습니다** — 토글은 소유자의 첫 시도에서 실패했고, 근거로
삼은 차일드락 쓰기도 지금 확인하니 거부됩니다. 그 주석은 기능을 하나 세우는 데 쓰였는데 아무도
다시 확인하지 않았습니다.

**철회한 것**: 전원 토글(→ 읽기 전용 `localthings_power_state`), 차일드락 쓰기(→ 읽기 전용
`localthings_child_lock_state`), 그리고 토글을 위해 만든 기계장치 전부(`Spec.refusal`,
`make_flow_cards.OFF_ONLY`, 전용 오류 메시지) — 쓰는 곳이 없는 추상을 남기지 않았습니다.
`lib/registry/shared.py`의 차일드락 분리 주석도 두 예시가 반증됐다는 사실과 함께 고쳤습니다.

**다시 열 조건**: 이 계열에서 4.05가 아닌 응답을 주는 기기. 페이로드는 위 표에 그대로
있으므로 다시 찾을 필요가 없습니다.

### `panDetection`과 버너 타이머 — 확정 (2026-08-04)

**타이머 단위는 초입니다 — 실기기로 확정.** 소유자가 화구 타이머를 걸었고, capability가 분을
정확히 읽고 카운트다운까지 따라갔습니다. 근거로 삼았던 가정(`safetyAlert.settingTime`이 1시간에
3600이므로 이 보드의 시간값은 초)이 맞았습니다.

**둘 다 "값 없음"을 `None`으로 반환하고 있었고, 그게 버그였습니다.** `None`은 Homey에 "이 값을
건드리지 말라"는 뜻이라 마지막 값이 그대로 남습니다. 그래서:

- 타이머를 취소해도 마지막 카운트가 계속 표시됨 → **0을 반환**하도록 수정
- 화구를 꺼도 냄비 "예"가 계속 표시됨 → **False를 반환**하도록 수정

두 번째는 특히 아이러니했습니다. 꺼진 인덕션이 세 화구 모두 `panDetection: true`를 보고하는
문제(읽기 3회에서 동일, 전원 off·화력 0)를 막으려고 게이트를 넣었는데, `None` 때문에 **막으려던
바로 그 화면**을 다른 경로로 만들었습니다. 소유자 보고: "화구를 켰을 때 예로 바뀌지만 화구를
취소했을 때 지워지지 않는다."

capability의 의미를 "**동작 중인 화구에 조리기구가 있는지**"로 확정했습니다. 유휴 상태의 `true`가
측정값이 아니라는 판단은 그대로 유효합니다 — 인덕션 코일은 전류가 흐를 때만 부하를 감지합니다.

### `/cooktop/recipe/status/vs/0` — 통째로 미매핑

```
operationBurnerNumber = 2, sequenceNumber, textData.menu = "",
stageInfo = {numberOfStages: 0, cookingTimeList: []}
```

스마트싱스 레시피(kitchenService) 연동용으로 보입니다. `menu`가 빈 문자열이고 단계가 0이라
채워진 상태를 본 적이 없습니다. 공식 앱에서 레시피 기능을 한 번 쓰고 다시 덤프하면 필드가
어떻게 채워지는지 알 수 있습니다. 그 전에는 매핑할 근거가 없습니다.

미매핑으로 남긴 나머지: `safetyAlert.settingTime`(차단 타이머 길이 — state만 노출),
버너 `mode`(항상 `normal`), `additionalStateList`(항상 `[""]`), 프로브의
`targetTemperature`·`batteryState`·`operationBurnerNumber`, `/quickcontrol/info/vs/0`.

---

## 위젯 — 한 번 만들었다가 뺐다

후드의 전원·풍량·조명·밝기를 한 카드에 놓는 위젯을 만들어 설치까지 했고, **보기에 좋지
않아서 제거했습니다.** 다시 시도할 때 계약을 처음부터 조사하지 않도록 알아낸 것을 남깁니다.

`widgets/<id>/` 아래에 네 가지가 필요합니다:

- `widget.compose.json` — `name`, `height`(숫자는 절대값, `%`는 종횡비), `api`,
  그리고 `devices`
- `public/index.html` — 진입점. `public/` 아래 파일은 Homey에서 호스팅됩니다
- `api.py` — `async def f(*, homey, query, params, body)` 시그니처, `__all__`로 내보냅니다
- `preview-light.png` / `preview-dark.png` — 필수

**기기 선택은 `devices` 설정이 합니다.** `{"type": "app", "singular": true, "filter":
{"capabilities": "..."}}` 형태이고, 뷰에서 `Homey.getDeviceIds()`로 받습니다. 클래스 필터는
쓰지 않는 게 맞습니다 — 후드의 `class`는 `fan`인데 공기청정기도 `fan`입니다.

**`Homey.ready()`가 로딩 상태를 없앱니다.** 첫 요청 전에 불러야 하고, 그러지 않으면 앱이
응답하는 동안 카드가 스피너로 남습니다.

**스타일은 Homey가 제공하는 것을 씁니다** — `--homey-su-*`(4px 배수 간격),
`.homey-button-primary-*`/`.homey-button-small`, `--homey-color-*`, `.homey-text-*`.
자체 색을 박으면 다크 모드에서 다른 카드와 어긋납니다.

**미리보기는 스크린샷이 아닙니다.** 심사 가이드라인 1.10: 텍스트 없음, 단순 도형, 투명 배경,
위젯 선택 화면 배경색과 같은 색 금지, 색 수 최소.

**요구 버전은 `>=12.3.0`** 이고 이 앱은 `>=13.0.0`이라 충족합니다. **Homey Cloud에서는 위젯이
동작하지 않습니다** — 이 앱은 `local` 전용이라 무관합니다.

다시 만들 때 유의할 점: 위젯 디자인은 대시보드의 다른 카드들과 나란히 놓였을 때 판단해야
하므로, 만들고 설치해서 실제로 보는 것 외에 검증 방법이 없습니다.

---

## 구조 — 드라이버는 하나로 유지한다 (결정됨)

한때 검증된 종류마다 드라이버를 따로 두는 안을 검토했습니다. **하지 않기로 했습니다.**
같은 조사를 반복하지 않도록 근거를 남깁니다.

### 확인된 제약 두 가지

**다른 드라이버의 기기를 만들 수 없습니다.** `PairSession`에도 `Driver`에도 `create_device`가
없고, 페어링 뷰의 `Homey.createDevice()`는 그 뷰가 속한 드라이버에만 기기를 만듭니다. 따라서
드라이버를 쪼개면 사용자가 "기기 추가" 목록에서 종류를 먼저 골라야 합니다. 이것만 보면 큰
문제는 아닙니다 — 사람은 자기 냉장고가 냉장고인 걸 알고, 종류가 안 맞으면 프로브 후 거부하면
됩니다.

**기존 기기는 옮길 수 없습니다.** `Device.driver`가 `Final`입니다. 쪼개도 이미 페어링된 기기는
전부 범용 드라이버에 남고, 전용 드라이버의 이점을 받으려면 재페어링해야 하며 그러면 플로우와
인사이트 이력을 잃습니다. **비용을 내는 사람과 이득을 받는 사람이 어긋납니다.**

### 얻는 것이 적습니다

쪼개려던 이유 네 가지 중 셋은 다른 방법으로 이미 해결됩니다.

| 이유 | 해결 상태 |
|---|---|
| 기기별 커스텀 뷰 | **위젯**이 해결합니다. 위젯은 캐패빌리티 필터로 기기를 고르므로 드라이버와 무관합니다 |
| `device_class` | `set_class`로 시작할 때마다 맞춥니다 (에어컨 thermostat, 후드 fan) |
| 기기 화면의 캐패빌리티 제목 | `Spec.titles`로 기기별 재정의됩니다 (후드 "조명" / 에어컨 "표시등") |
| 드라이버 아이콘이 하나 | 쪼개야 해결. 순수 미관 |
| 플로우 카드 문구 | 쪼개야 해결. 공용 캐패빌리티 **한 개**(`localthings_display_light`)에만 영향 |

남는 실익은 아이콘과 카드 문구 하나이고, 대가는 아이콘 4개 + 스토어 이미지 12장입니다.
가이드라인 1.6이 드라이버마다 **고유** 아이콘을 요구하므로 재사용도 불가합니다.

### 그래도 해 둔 것

구현을 `lib/appliance/{driver,device}.py`로 옮기고 `drivers/appliance/*.py`는 15줄 shim으로
만들었습니다. **상속이 Homey 파이썬 런타임에서 동작하는 것도 확인했습니다**(기기 9대 정상).

그래서 나중에 쪼갤 이유가 생기면 드라이버 하나가 15줄 + 에셋 + `$extends` 템플릿입니다. 결정을
미룰 수 있게 만들어 둔 것이 이 리팩터링의 성과입니다.

**다시 검토할 조건**: 검증된 종류가 훨씬 늘어 범용 아이콘 하나가 실제로 혼란을 주기 시작할 때,
또는 Homey가 기기 이전이나 드라이버 간 생성을 지원하게 될 때.

---

## 레퍼런스에서 아직 이식하지 않은 것

### 다중 실내기 / subdevice (`#177`, `#205`, `#214`, 611줄)

2-in-1 / 시스템 에어컨이 하나의 IP·하나의 DTLS 세션 뒤에 논리적 실내기를 여러 개 두는 경우.
두 가지 방식(인덱스형 `/device/<n>`, UUID 접두사형 `/subdevices/vs/0`)이 있습니다.

**보유 기기 9대 전부에 해당 신호가 없습니다** — `/subdevices/vs/0`, `/device/1`, `/mode/vs/1`
모두 부재. discovery·coordinator·adapter·platform 4개 경계를 건드리는 변경이라 검증 없이
이식하지 않습니다. 해당 기기 사용자의 덤프가 필요합니다.

**v0.17.2 재확인 (2026-07-31).** 레퍼런스가 이 영역을 `subunits.py` → `subdevices.py`로
개편하며 492줄에서 611줄로 늘었습니다. 이번에 추가된 것:

- **flat-href 폴백** — 접두사형 subdevice에 `/device/0` Collection이 없으면 href를 개별로
  폴링 (`#205`가 그 하드웨어에서 간헐 무응답을 보고한 건과 맞물립니다)
- **liveness 게이트** — 살아 있는 상태가 전력 계량기뿐인 슬롯은 실체화하지 않음.
  `#214`의 "유령 두 번째 에어컨"이 바로 이 게이트가 없어서 생긴 기기입니다
- **`async_remove_config_entry_device`** (`#214`) — HA 고유. HA는 로드된 config entry에
  속한 기기를 UI에서 지울 방법이 없어서 이 핸들러를 정의해야 삭제 버튼이 생깁니다.
  **Homey에는 해당 사항이 없습니다** — 기기 삭제가 항상 가능하고, 애초에 유령 기기를 만들
  subdevice 코드가 없습니다

설치된 앱의 `/resources`로 9대를 다시 훑어 `subdevice`·`numofsubdevice`를 포함하는 href나
필드가 하나도 없음을 재확인했습니다(추정이 아니라 실측). Homey에서는 subdevice 1개당 Device
1개가 되어야 자연스러운데 SDK가 드라이버 간 `create_device`를 제공하지 않으므로, 이식은
기기 모델 자체를 다시 설계하는 일입니다. 검증할 하드웨어가 없는 상태에서 할 작업이 아닙니다.

### `/oic/d` 실측 — 채택 완료, 한 종류만 보류

레퍼런스가 v0.17.2 이후 기기 종류 판정을 `/oic/d`의 `rt`로 옮겼습니다(보드 파트넘버 파싱보다
앞서 판정). 우리도 채택했고, 채택 전에 실기기 9대를 측정했습니다.

| 대수 | `rt` | 기기 |
|---|---|---|
| 4 | `oic.d.airconditioner` | `AJ023CN1UBC1`, "Samsung System A/C" |
| 3 | `oic.d.refrigerator` | "[refrigerator] Samsung" |
| 1 | `oic.d.cooktop` | `TP1X_DA-KS-COOKTOP`, "Samsung Cooktop" |
| 1 | `x.com.st.d.hood` | `AHD-WW-TP1-22-COMMON`, "Samsung Hood" |

**9대 전부가 값을 채우고, 전부 기존 보드 토큰 판정과 일치했습니다.** 즉 우리 집 기기에서는
아무것도 바뀌지 않습니다. 이득은 **다른 사람 기기**에서 나옵니다 — 낯선 modelNum은 지금
"지원되지 않는 기기"로 끝나는데, 이제 독립적인 두 번째 판정 경로가 생겼습니다.

측정에는 `/read-resource` 엔드포인트를 새로 만들었습니다. `/oic/d`는 **모든 `/device/0` 배치
응답에 없어서**(Collection이 아님) 직접 GET하지 않으면 답을 알 수 없었습니다.

**`oic.d.cooktop`은 의도적으로 매핑하지 않았습니다.** 우리 인덕션이 이 값을 보고하지만,
`cooktop`(가스, NA9300K — 버너 상태가 `/mode/vs/0`의 options 배열)과 `induction_cooktop`은
영어 단어만 같은 별개 레지스트리입니다. OCF 타입으로는 구분이 안 되므로 어느 쪽에 매핑해도
다른 쪽을 잘못 판정하고, **1순위 신호이므로 제대로 맞힌 보드 토큰을 덮어씁니다.** 이 둘은
보드 토큰이 계속 판정합니다.

레퍼런스 테이블에 없던 `x.com.st.d.hood`와 이 `oic.d.cooktop` 주의사항은 업스트림에
기여했고 **머지됐습니다** — [mbillow/localthings#230](https://github.com/mbillow/localthings/pull/230).

### 초기 알림이 통째로 유실되는 간헐적 모드 (미해결, 실측 2026-08-03)

**증상**: 같은 기기·같은 코드·같은 꺼진 상태에서 몇 시간 사이에 `0/27`과 `27/27`이 모두
나옵니다. 가전 쪽에서 바뀐 것은 없습니다.

| 손님방 에어컨 (Off, `onoff: false`) | 알림 |
|---|---|
| 재설치 반복 직후 | **0/27** → polling |
| 테스트 빌드를 쓴 뒤 재설치, 2분 경과 | **27/27** → push |

꺼진 에어컨은 상태 변화를 만들지 않으므로 **27개는 등록 시 초기 알림일 수밖에 없습니다.**
그러므로 초기 알림은 실재하고, 전송 라이브러리의 `subscribe` docstring이 맞습니다. 그리고
27/27이 가능하다면 0/27은 **완전 유실**입니다.

같은 라운드에서 인덕션은 **요리 중**(`burner_state.1: running`)인데 `0/4`였습니다. 활동량으로
설명되지 않습니다.

**후보 두 가지를 실험으로 검증했고, 둘 다 지지되지 않았습니다** (2026-08-03, 4라운드).

꺼진 에어컨 두 대가 깨끗한 계측기입니다 — 변화를 만들 수 없으므로 그 카운트는 순수하게 초기
알림입니다. 매 라운드 **설치·재시작 후 90초**라는 같은 시점에서 측정했습니다.

| 라운드 | 전체 | 예린방 (Off) | 손님방 (Off) |
|---|---|---|---|
| 냉정 시작 (앱이 한동안 없었음) | 126/148 (85%) | 27/27 | 27/27 |
| A1 연속 재설치 | 74/148 (50%) | 27/27 | 16/27 |
| A2 재시작 | 118/148 (80%) | 27/27 | 21/27 |
| A3 재시작 | 97/148 (66%) | **8/27** | 27/27 |

- **고아 관측 누적** — 연속 재시작이 단조 악화해야 합니다. 50% → 80% → 66%. **아닙니다.**
- **재설치 직후 vs 냉정 시작** — A2(80%)가 냉정 시작(85%)과 사실상 같습니다. 4라운드로는
  노이즈와 분리되지 않습니다. 애초에 이 절을 열게 한 `0/27` 대 `27/27` 대비는 재설치 시점이
  아니라 **넓은 분포의 양 끝**을 본 것으로 설명하는 편이 단순합니다.
- **등록 버스트**도 이것만으로는 같은 조건에서 8과 27이 갈리는 것을 설명하지 못합니다.

**실제로 확립된 것은 편차입니다.** 꺼진 같은 기기가 같은 시점에서 27 → 27 → 27 → 8을
기록합니다. 초기 알림은 실재하되 전달이 라운드마다 크게 요동칩니다.

**따라서 고칠 것은 임계값이 아니라 판정 방식입니다.** 한 라운드 스냅샷으로 채널을 판정하는
구조 자체가 이 하드웨어에서 성립하지 않습니다 — 아무것도 바뀌지 않아도 27/27과 8/27이
연달아 나옵니다. `OBSERVE_SUCCESS_FRACTION`을 어떤 값으로 두든 이 편차를 넘지 못합니다.
아래 스윕 기반 누락 감지는 등록 시점 전달에 의존하지 않으므로 이 문제를 우회합니다.

`homey api raw -X POST --path /api/manager/apps/app/<id>/restart`로 라운드를 싸게 반복할 수
있습니다. 앱 비활성화 엔드포인트는 없고(`/disable`은 404), `PUT`으로 `enabled: false`를 보내도
적용되지 않습니다. 언인스톨은 페어링된 기기가 사라지므로 실험에 쓰지 않습니다.

**카운터를 읽을 때 주의**: `_notified`는 재구독 때만 비워지므로 **초기 알림과 그 뒤의 변화
알림을 함께** 셉니다. 재구독 없이 꺼진 기기를 켜자 70초에 0 → 7이 되었고 `silent_rounds`와
`retry`는 움직이지 않았습니다. 따라서 **두 기기의 값은 마지막 재구독으로부터 같은 시점에서만
비교 가능합니다.** 이걸 무시해서 하루 동안 양방향으로 틀렸습니다 — 먼저 "버스트가 초기
알림을 잃는다", 다음엔 "가전은 초기 알림을 아예 안 보낸다"로 과잉 정정.

실질 피해는 작습니다. `_observing`이 바꾸는 것은 폴링 간격뿐(30초 ↔ 300초)이고 틀리는 방향이
**안전한 쪽**입니다. 그래서 동작은 그대로 두었습니다.

### 푸시가 죽어도 강등되지 않습니다 (신호 설계 미정)

`_push_is_healthy()`는 정의만 되어 있고 호출되는 곳이 없어 **삭제했습니다**(상수
`PUSH_HEALTH_WINDOW_S`도 함께). 문제 자체는 남아 있습니다 — 한 번 push로 인정된 기기는
채널이 죽어도 `OBSERVE_REFRESH_S`(6시간) 동안 재검증되지 않습니다.

**그 함수를 그대로 되살리면 안 됩니다.** 판정 기준이 "최근에 알림이 왔는가"인데, 꺼져 있고
조작이 없는 가전은 정상이어도 조용합니다. 침묵을 고장으로 읽으면 유휴 기기를 계속
강등·재구독시킵니다.

필요한 신호는 **5분 요약 스윕이 push가 놓친 변화를 발견했는가**입니다. `_apply`가 이미 변화를
감지하므로 관측 중인 href에서 스윕이 변화를 찾으면 실제 누락입니다. 다만 착오 신호가 두 가지
있어 그것까지 처리해야 합니다.

- `_settling` 창은 4초, 스윕은 300초 간격이라 스윕 시점엔 모든 창이 만료돼 있습니다. 쓰기 직후
  판정을 배제하려면 "4초 내"가 아니라 **"이전 스윕 이후에 쓰였는가"**로 추적해야 합니다.
- `_on_notification`은 settling 중 알림을 **버립니다**(보류가 아니라). 그 변화에 대한 유일한
  알림이었다면 다음 스윕이 차이를 발견해 "push가 놓쳤다"고 오판합니다.

먼저 24시간 유휴 기준선으로 `sweep_detected_misses`를 세어 임계값 N을 정한 뒤 구현합니다.
계획은 `.omc/plans/device-comm-layer.md`.

**이 지표는 승격 쪽에도 도움이 됩니다.** `OBSERVE_SUCCESS_FRACTION`은 채널 검사로서 정당하지만
(위 절 참고) 위상에 민감하고 완전 유실 앞에서 무력합니다. 스윕 기반 누락 감지는 "알림이 몇 개
왔는가"가 아니라 "놓친 변화가 있는가"를 재므로 재구독 시점과 무관하고, 유휴 기기에도
성립합니다. 승격·강등을 한 지표로 판정할 수 있게 되는 것이 이 항목의 진짜 가치입니다.

### 업스트림에 기여해 머지된 것

| PR | 내용 |
|---|---|
| [#230](https://github.com/mbillow/localthings/pull/230) | `x.com.st.d.hood` → `range_hood`, 그리고 `oic.d.cooktop`이 가스/인덕션을 구분하지 못한다는 주의사항 |
| [#255](https://github.com/mbillow/localthings/pull/255) | 자동건조 사이클 상태와 진행률 (`/option/autoclean/vs/0`의 `status`·`progress`) |

레퍼런스도 `settingStatus`만 읽고 있었고, 실기기에서 `Start`/98% → `Stop`/0%를 관측해
근거로 냈습니다. 둘 다 우리 앱에는 이미 들어가 있습니다.

기여하지 않은 것: `oic.d.cooktop` 매핑 자체(모호해서 우리도 안 씀), 자가진단 `progress`
(같은 구조지만 실행 중인 값을 못 봐서 뺐습니다 — PR 본문에 그렇게 적었습니다).

### 아무것도 읽지 못하는 매핑 6종 — 수정 완료 (2026-08-04)

**"레퍼런스에는 있는데 우리에겐 없는 기기가 있나?"에서 나온 것입니다.** 기기 종류는 18종 전부
라우팅되어 있었지만, 그중 6종이 **존재하지 않는 경로나 필드에 묶여 있었습니다.** 페어링도 되고
capability도 생기고 테스트도 통과하는데 값은 영원히 비어 있습니다 — 읽기가 `None`을 반환하면
"건드리지 말라"는 뜻이라 예외가 나지 않기 때문입니다. 주방 후드가 필드명을 전부 틀린 채로
출시됐던 그 실패 양상입니다.

| 종류 | 우리가 쓰던 것 | 실제 (레퍼런스 실기기 덤프) |
|---|---|---|
| 청정스테이션 | `/cleanstation/status/vs/0`, `/dustbag/vs/0` | `/status/cleanstation/vs/0`, `/component/station/dustbag/vs/0`, `/component/station/dustbagusage/vs/0` — **필드명 3개도 전부 틀렸습니다.** 이 종류는 아무것도 보고할 수 없었습니다 |
| 정수기 | `/lock/vs/0`, `/status/vs/0` | `/status/lock/vs/0`(잠금 3종, `Locked`/`Unlocked`), `/status/waterpurifier/vs/0` |
| 에어드레서 | `/airdresser/vs/0` | `/airdresseroption/sanitize/vs/0`, 설정은 `/washer/vs/0` |
| 오븐·전자레인지·레인지 | `/door/vs/0`, 그리고 `/oven/vs/0`의 `operationState`·`mode`·`temperature`·`desiredTemperature` | `/doors/vs/0`(items[]), `/oven/vs/0`의 `state`, 모드는 `/mode/vs/0`의 `modes`, 온도는 `/temperatures/vs/0`의 items[] |
| 모든 세탁 계열 | `remainingTime`을 숫자 문자열로 파싱 | **덤프 20개 전부 `HH:MM:SS`입니다.** 3시간 35분 남은 세탁기가 아무 값도 안 내놓고 있었습니다 |

정수기 잠금은 위의 "정수기 잠금 리소스 불일치" 항목에서 *"실기기 없이는 알 수 없다"*고 미뤄
뒀던 것입니다. **레퍼런스의 실기기 덤프가 답이었습니다** — `/status/lock/vs/0`이 있고
`/lock/vs/0`은 없습니다. 우리에게 하드웨어가 없는 종류에는 레퍼런스의 커밋된 덤프가 가장
하드웨어에 가까운 증거이고, 그걸 읽지 않고 있었습니다.

레인지 덤프는 **화씨**로 보고합니다(`unit: Fahrenheit`, 175). 레퍼런스는 기기 단위를 그대로
붙여 내보내지만 Homey의 `measure_temperature`는 섭씨 전용이라 **변환**해야 합니다 — 175를 그냥
내보내면 79 °C인 오븐이 175 °C로 표시됩니다.

`tests/test_no_dead_mappings.py`가 이 부류를 영구적으로 막습니다: (1) 아무도 보고하지 않는
경로를 묶지 않는다, (2) 어떤 spec도 자기 종류의 **모든** 덤프에서 비어 있지 않다. 일부 덤프에서
비는 건 정상이고(그 보드가 그 필드를 안 보냄), 전부에서 비면 매핑이 틀렸거나 확인 불가라는
뜻이므로 이유를 적어 허용 목록에 넣거나 고쳐야 합니다. 현재 허용 목록 14건은 전부
"누적전력만 보고" / "프로브 미연결" / "유휴라 설정온도 0" 같은 정당한 사유입니다.

### 레퍼런스 덤프로 추가한 값 (2026-08-04)

죽은 매핑을 고친 뒤, 같은 덤프들을 근거로 **읽을 수 있는데 안 읽던 값**을 찾았습니다.

| 추가 | 근거 |
|---|---|
| 후드 주기적 공기질 감지 4종 — 감지 활성 여부, 감지 상태, 최근 등급, 감지 시 동작 | 우리 후드 덤프의 `/airlevelcheck/vs/0`. 레퍼런스도 **후드에만** 노출하고 에어컨·공기청정기에서는 "스케줄러 배관"이라며 의도적으로 뺐습니다 — 우리도 후드에만 붙였습니다. **2026-08-05에 공기청정기와 에어컨으로 넓혔습니다**(레퍼런스가 공기청정기에서 판단을 뒤집었고, 에어컨은 우리 4대 실측이 근거) — 아래 동기화 기록 참조 |
| 청정스테이션 자동 비움·자동 닫힘 (쓰기) | 레퍼런스 실기기 덤프의 `/setting/dustbin/vs/0`, `On`/`Off` |

**감지 시 동작(`autoExeState`)은 enum이 아니라 문자열입니다.** 우리 후드는
`autoExeState = "Sensing"`인데 자기 `supportedAutoExeState`에는 `["Airpurify","Alarm"]`만
들어 있습니다 — 지원 목록으로 만든 선택기는 **가전이 실제로 들어가 있는 값을 표시조차 못
합니다.** `Kr1` 등급의 척도는 여전히 미확인이라 숫자로 바꾸지 않고 그대로 보여줍니다.

**확인해보고 추가하지 않은 것** (추측이 아니라 측정 결과입니다):

- 에어컨 `/temperature/control/vs/0`의 `increment` — 이미 `/temperatures/vs/0`에서 같은 값을
  읽어 슬라이더 간격에 쓰고 있습니다. 중복입니다.
- 냉장전용 냉장고의 `/refrigeration/0` — `rapidCool`/`rapidFreeze`가 있지만 벤더 형식
  `/refrigeration/vs/0`을 이미 읽고 있어 급속냉장이 이미 붙습니다. `defrost`만 미매핑입니다.
- 냉장고 `/status/lock/vs/0`의 `ado.soundcontrol` — 3대 모두 `On`입니다. 레퍼런스는 이 리소스의
  **다른** 필드(`ado.devicecontrol`, `device.sound`)를 쓰고 이 필드는 아무도 안 씁니다. 의미가
  확정되지 않아 보류했습니다. **확정 방법**: 삼성 앱에서 자동문/문열림 소리 설정을 바꾸고 이
  값이 따라 바뀌는지 보면 됩니다. 그때 쓰기까지 붙일 수 있습니다.
- 냉장고 `/autodoor/timer/vs/0` (`time.desired`, `supportedOptions`) — 레퍼런스도 매핑하지
  않았고 단위·의미가 불명입니다. 선택지가 기기에서 오는 enum이라 Homey 정적 선언 문제도 같습니다.
- 에어컨 `/aisleep/vs/0`, `/settings/sound/optimization/vs/0`, 후드 `/autoventilation/vs/0` —
  레퍼런스도 노출하지 않거나(전자) 의미가 불명확합니다(후자, `action = "Start"`가 명령인지
  상태인지). 값 영역이 확실해지면 언제든 붙일 수 있습니다.
- 세탁 계열 코스 카탈로그(`/course/vs/0`, `/wm/editcourse/vs/0`) — 보드 세대별 라벨 테이블
  전체가 필요하고, 레퍼런스도 세대 분기로 고생하는 영역입니다. 실기기 없이 들일 이유가 없습니다.

### 동기화 기록 — v0.19.0 → `4e47a1c` (2026-08-05)

머지 제외 14개 커밋을 전부 읽고 판정했습니다. 디프 줄 수의 대부분은 주석 정리 한 커밋이고,
기능 변경은 다섯 갈래뿐입니다.

| 레퍼런스 커밋 | 판정 |
|---|---|
| `/device/0` 첫 항목을 버리던 파싱 (`3d0dca2`) | **적용 — 우리 실기기 버그였습니다.** 아래 항목 참조 |
| AI Purify 감지 엔진 `/airlevelcheck/vs/0` (`412fff9`, `a5484f7`, `96d0636`) | **부분 적용.** 공기청정기와 **에어컨**에 읽기 4종을 붙였습니다. 쓰기(활성 토글·감지 동작·주기·건너뛰기 창)는 미적용 — 아래 항목 참조 |
| `good_sleep`이 시간이 아니라 30분 단위 (`93f45cb`) | **해당 없음.** `Sleep_` 토큰을 아예 바인딩하지 않습니다. 우리가 `/mode/vs/0`의 `options[]`에서 읽는 토큰은 `SmartCoolClean`과 `ProgressSmartClean` 둘뿐입니다 |
| 자동 청소 중단·진행률 (`290a348`) | **해당 없음 (같은 이유).** `Autoclean_`·`AutocleanProgress_`·`StopAutoClean_`은 레거시 ARTIK051 보드의 options 토큰입니다. 우리 CAC는 같은 기능을 `/option/autoclean/vs/0`에서 읽고, 설정·동작 여부·진행률 3종이 이미 있습니다 |
| 레거시 필터 카운터 리셋 (`9ee0329`) | **미적용.** `FilterTime_` 토큰을 바인딩하지 않고, 이 토큰을 쓰는 보드를 아무도 갖고 있지 않습니다. 레퍼런스는 ARTIK051_KRAC_18K 실기기에서 확인했지만 우리에겐 확인할 기기가 없고, 검증 없는 쓰기는 1.0.3 인덕션 토글로 이미 한 번 냈습니다 |
| 냉장고·쿡탑 `entity_category` `config`→`diagnostic` (`3d0dca2`) | **해당 없음.** HA 고유 개념으로, Homey에 대응물이 없습니다 |
| 주석 가이드라인 + 대량 정리 (`7086b13`) | **원칙만.** 이번 디프 3,900줄 중 대부분이 이 커밋입니다 |
| 한국어 번역과 그 보정 (`a38437f`, `3918b1e`, `e6909fb`), ITA 오타 (`330b2f3`) | **해당 없음.** 우리 `ko`는 별도로 관리합니다 |
| `README.md` 갱신 (`54a676f`), ruff 포맷 (`75e985d`) | **해당 없음** |

인증서 도구 레퍼런스(`smartthings-local-reference`)는 변경 없음(`Already up to date`).

#### `/device/0` 첫 항목 — 인덕션이 리소스 하나를 잃고 있었습니다 (수정, 2026-08-05)

`parse_device0`은 항목 [0]을 무조건 건너뛰었습니다. 배치 응답이 기기 수준 컬렉션 표현으로
시작한다는 전제였고, 여기 대부분의 펌웨어는 실제로 그렇습니다. **인덕션은 아닙니다** —
`/connectionconfig/vs/0`으로 바로 시작하므로 그 리소스가 매 폴링과 매 덤프에서 버려졌습니다.
아무 증상도 없었습니다: 지도에서 빠진 리소스는 가전이 호스팅하지 않는 리소스와 구별되지
않고, 그래서 `test_no_dead_mappings.py`가 존재하는 것과 같은 실패 양상입니다.

실측(2026-08-05, 원본 `/device/0`을 9대 전부에 대해 한 번의 호출로 읽음):

| [0]의 모양 | 기기 |
|---|---|
| `{}` | 냉장고 3대, 후드 |
| `{"rt": [...], "if": [...]}` | 에어컨 4대 |
| `{"href": "/connectionconfig/vs/0", "rep": {...}}` | **인덕션** |

앞의 두 모양은 `href`가 없으므로 [0]을 보든 안 보든 기존 검사가 걸러냅니다. 인덕션은
13개 항목을 돌려주는데 우리 커밋된 덤프에는 12개가 있었습니다 — 레퍼런스의 NV9000D 픽스처
주석("this firmware starts directly with `/connectionconfig/vs/0`")을 우리 기기에서 확정한
산수입니다. 같은 보드 계열(`TP1X_DA-KS-COOKTOP-01011`)이고, 레퍼런스는 KO2, 우리는 KO4.

전체 순회로 바꾸고, 잃었던 리소스를 픽스처에 채우고, `tests/test_device0_parsing.py`에
양쪽을 고정했습니다 — 세 번째 모양에서 리소스를 잃지 않을 것, 앞의 두 모양에서 없는 리소스를
만들어내지 않을 것. 옛 구현을 새 입력에 돌려 실제로 빨간불이 되는지 확인했습니다.

우리 픽스처는 **파싱 후** 형태(`{href: rep}`)로 저장되므로 이 버그를 잡을 수 없었습니다.
`parse_device0`을 통과하는 픽스처가 하나도 없다는 뜻이기도 합니다.

#### 공기질 감지 — 읽기만 붙였습니다 (쓰기 미적용)

레퍼런스는 활성 토글과 감지 동작을 쓰기 가능하게 만들면서, 쓰기를 **AVT-WW-TP1-23
하드웨어에서만** 실행했고 "the other two families get the same writes on field-shape
grounds only"라고 직접 적었습니다. 우리는 공기청정기를 아무도 갖고 있지 않고, 에어컨은
그 "other two families" 쪽입니다. 주기(초 단위)와 하루 건너뛰기 창(`HHMMHHMM` 한 문자열)도
새 capability와 플로우 카드가 필요해 함께 보류했습니다.

에어컨 4대 실측(2026-08-05)이 읽기를 붙인 근거입니다:

```
periodicSensingActivationState  On            (4/4)
sensingState                    NonProcessing (4/4)
lastSensingLevel                Kr2, Kr2, Kr2, Kr1
autoExeState                    Alarm         (4/4)
supportedAutoExeState           ["Airpurify", "Alarm", "Sensing"]
```

레퍼런스는 에어컨에선 여전히 "스케줄러 배관"이라며 빼 두는데, 이 계열에서는 배관이 아닙니다.
이 스케줄이 이미 바인딩된 미세먼지·공기질 값을 샘플링하는 주기이고, 4대 모두 기본값이 아니라
동작이 설정된 상태입니다. **의도적 분기이므로 새 측정 없이 되돌리지 마십시오.**

재개 조건: 감지 동작 쓰기는 우리 에어컨이 이 계열 중 유일하게 보고값이 자기 지원 목록 안에
있는 경우입니다. 한 대에 `autoExeState` 쓰기를 넣고 재접속 후에도 남는지 읽어보면 끝납니다 —
확인되면 선택기로 승격할 수 있습니다. 후드는 `supportedAutoExeState`에 없는 값을 보고하므로
지원 목록으로 만든 선택기를 세 종류에 공유하는 것은 그 확인 뒤에도 안전하지 않습니다.

#### 에어컨 `autoExeSetting` — 미매핑 (신규, 2026-08-05)

위 실측에서 처음 보였습니다. 우리 에어컨 4대는 후드와 공기청정기에 없고 **레퍼런스가 어느
타입에서도 모델링하지 않는** 필드 한 쌍을 함께 보고합니다:

```
autoExeSetting           = "Off"
supportedAutoExeSetting  = ["On", "Off"]
```

`autoExeState`(Airpurify/Alarm/Sensing)와 이름이 겹치지만 값 영역이 다릅니다. 무엇을
켜고 끄는지 불명이라 바인딩하지 않았습니다. 정할 실험: 한 대에서 `On`으로 바꾸고
`autoExeState`와 감지 주기가 따라 움직이는지 봅니다.

### 공기청정기 — 블루스카이 5500 동작 제보 (2026-08-05)

사용자 제보로 **블루스카이 5500 공기청정기가 동작한다**는 것을 받았습니다. 여기에는 공기청정기
실물이 없으므로 이것이 이 타입에 대한 유일한 실기기 신호입니다. README 지원 표와 `CLAUDE.md`에
**제보로** 기록했고, 검증 4종에는 넣지 않았습니다.

왜 승격시키지 않았는지: "동작한다"는 페어링이 되고 무언가 표시됐다는 뜻이고, 그것으로는 맞는
값과 그럴듯하게 틀린 값이 구별되지 않습니다. 후드가 정확히 그렇게 출시됐습니다 — 모든 필드가
틀린 채 페어링되고 유닛 테스트는 통과했습니다.

**"블루스카이 5500"은 단일 모델이 아닙니다.** 마케팅 시리즈명이고 세대가 걸쳐 있습니다
(모두 60㎡/18평형):

| 모델명 | |
|---|---|
| `AX060CG500GBD` / `GED` / `GGD` | 색상·구독형 변형 |
| `AX060B510RPD` | 이전 세대 |
| `AP70F06103RVD` | 최신 명명 체계 |

제보에 보드 토큰이 없어 `AIR`/`TVTL`/`VTWW`/`AVT` 중 무엇으로 붙었는지 모릅니다. 소비자
모델명으로는 알 수 없습니다 — 라우팅은 `/information/vs/0`의 보드 문자열로 하고, 레퍼런스
덤프 4종이 그 차이를 보여줍니다:

```
AVT  AVT-WW-TP1-23-AXX500        (BESPOKE 큐브 에어)
TVTL ARTIK051_TVTL_18K
VTWW A-VTWW-TP2-21-COMMON
AIR  TP1X_DA-AC-AIR-01031_0000
```

**확인되지 않은 단서 하나**: `AVT-WW-TP1-23-AXX500`의 `AXX500`이 `AX060…500` 제품군과
맞물려 보입니다. 이름에서 유도한 추론이므로 **근거로 쓰지 마십시오** — 이 저장소가 이름의
시사를 믿고 낸 사고가 이미 두 번입니다. 보드 문자열과 소비자 모델명이 대응한다는 확인은
어디에도 없습니다.

정할 실험: 제보자에게 `/device/0` 덤프(또는 최소한 `/information/vs/0`의 `description`)를
받습니다. 그러면 세 가지가 한 번에 정해집니다 — 어느 보드 토큰인지, 우리 매핑이 그 보드에서
실제로 값을 읽는지, 그리고 난독화 픽스처로 커밋해 `test_no_dead_mappings.py`에 넣을 수
있는지. 그 덤프가 이 행을 "실기기 검증"으로 올릴 수 있는 유일한 근거입니다.

### 동기화 기록 — v0.18.0+36 → v0.19.0 (2026-08-04)

레퍼런스 16개 커밋을 전부 읽고 판정했습니다. **다음 동기화 때 이 표를 먼저 보면 같은 조사를
반복하지 않습니다.**

| 레퍼런스 커밋 | 판정 |
|---|---|
| AC UV LED·환기 알림·PM1 필터 (`#270`) | **부분 적용.** UV LED는 이미 있었고, PM1 필터의 `filterStatus`가 누락돼 있어 `localthings_alarm_filter.pm1`을 추가했습니다(실기기가 이 필드를 보고합니다). 환기 알림은 위 항목 참조 |
| 제습기 화면·물통 조명 (`#271`, `#231`) | **부분 적용.** `/display/vs/0`와 물통 조명 켜기/끄기·알림 상태를 이식, 색상·밝기는 위 항목 참조 |
| TP1X_REF 이산 냉동 설정값 (`#229`) | 미적용 — 위 항목 참조 |
| 식기세척기 Normal/Express 60 라벨 교정 (`#226`) | **해당 없음.** 코스 라벨 카탈로그를 아예 이식하지 않았습니다 |
| 건조기 코스 라벨 추가 (`#244`) | **해당 없음.** 같은 이유 |
| 건조기 Drum Clean+ 추적 (`#258`) | **해당 없음.** `/course/vs/0`의 `options[]`를 세탁기에서도 읽지 않습니다 |
| HOMECARE_WIZARD_V2 오탐 수정 (`#235`) | **해당 없음.** 홈케어 위저드를 이식하지 않았습니다 |
| v1 마이그레이션 시리얼 재키 수정 | **해당 없음.** HA 엔트리 마이그레이션 고유 문제 |
| 기기 검색을 ClientHello 프로브로 재구성 | 미적용. 우리 `lib/probe.py`는 이미 포트 스윕 + DTLS 생존 게이트를 쓰고 실기기 9대에서 검증됐습니다. 정체성 선확인도 이미 시리얼로 합니다 |
| "cannot connect" → 실패 분류 체계 | **적용(우리 실패 양상으로).** `ProbeFailure` 4종. 인증서 판정 기준은 레퍼런스와 **다릅니다** — 이 하드웨어는 핸드셰이크를 완료한 뒤 응답을 안 하는 방식으로 인증서를 거부하므로(경보를 보내지 않습니다), 세션이 올라왔는지로 갈랐습니다 |
| 카탈로그 리터럴 테스트 제거 + 안티패턴 문서화 | **원칙만 채택.** 우리 쪽에 해당 테스트가 없습니다(`test_i18n.py`는 불변식을 검사합니다). 새로 쓴 테스트도 생성기 출력이 아니라 **의도**를 별도로 적어 실패할 수 있게 뒀습니다 |
| `cs.json` 줄바꿈 정규화 → 복원 (2건) | **해당 없음.** 우리 파일이 아닙니다. `CLAUDE.md`의 CRLF 주의사항은 그대로 유효합니다 |
| `ty` 타입 오류 수정 | **해당 없음** |

인증서 도구 레퍼런스(`smartthings-local-reference`, v0.1.2+12)의 9개 커밋은 CI·배포
검사와 LibreSSL SHA-1 재시도 호환성입니다. **우리에게 해당 없습니다** — 가전이 서명을
확인하지 않는다는 실측 이후 앱이 인증서를 직접 발급하므로 SHA-1도 pyOpenSSL도 쓰지 않습니다.
`docs/CA-SETUP` 절차를 따르는 사용자에게는 그쪽 저장소가 최신이면 그만입니다.

### EHS 열펌프 미매핑 리소스

`TP1X_DA_AC_EHS` (공기-물 열펌프). 전원·현재온도·희망온도만 이식했습니다. 나머지는 새
capability와 아이콘이 필요한데, **실기기가 없어 확인할 수단이 없습니다.**

| 리소스 | 내용 | 보류 이유 |
|---|---|---|
| `/mode/vs/0` | zone 운전 모드 | 값 집합이 에어컨과 다릅니다. `supportedModes`를 실기기에서 봐야 enum을 정의할 수 있습니다 |
| `/mode/dhw/vs/0` 외 | 온수(DHW) 루프 전체 — 전원·모드·온도 | 별도 루프라 capability 세트를 새로 만들어야 합니다 |
| `/option/outgoing/vs/0` | 외출 모드 (`x.com.samsung.da.away`) | 매핑은 명확하지만 맞는 아이콘이 저장소에 없고, 아이콘 path를 지어낼 수는 없습니다 |

설정 온도는 실내 온도가 아니라 **출수 온도**입니다 — 레퍼런스가 이 리소스의 `type=Water`가
문자 그대로의 수온이 아니라고 적어 둔 이유입니다. 기본 범위 5~30℃(0.5 단위)는 레퍼런스 값을
따르고, 보드가 자기 `minimum`/`maximum`/`increment`를 보고하면 그쪽이 우선합니다.

### 에어모니터 미매핑 리소스 (`#210`)

`ASM-KR-TP1-22` 보드에서 레퍼런스가 매핑한 것 중 둘을 보류했습니다.

| 리소스 | 보류 이유 |
|---|---|
| `/dnd/vs/0` | 시작/종료 시각 쌍. Homey에 대응하는 시각 capability 타입이 없어 표현 방법부터 정해야 합니다 |
| `/airqualitystandard/vs/0` | 기준 이름 문자열(`x.com.samsung.da.standard`). 값 도메인이 확인되지 않았습니다 |

배터리 충전 여부(`x.com.samsung.da.charging`)도 뺐습니다 — Homey에 대응하는 시스템
capability가 없고, 커스텀으로 만들 만큼 확인된 값이 아닙니다. 실기기가 없는 종류입니다.

### 정수기 잠금 리소스 불일치

우리 정수기 레지스트리는 `/lock/vs/0`의 `status`를 차일드락으로 쓰기까지 합니다. 그런데
레퍼런스 현재 버전의 정수기 잠금은 `/status/lock/vs/0`의 `x.com.samsung.da.hotwaterLock`
이고 이름도 **온수 잠금**입니다. 둘 중 어느 쪽이 맞는지 실기기 없이는 알 수 없어 기존
매핑을 그대로 뒀습니다. 정수기 덤프가 생기면 확정합니다.

### 정수기 진단 리소스 (`#196`)

레퍼런스가 AILITE 보드(RWP70F15ANW)에서 새로 매핑한 것 중 셋을 보류했습니다.

| 리소스 | 보류 이유 |
|---|---|
| `/cup/state/vs/0` | 관측된 값이 `UnReady` 하나뿐. 레퍼런스도 상태 도메인 미확인이라 enum이 아닌 평문 센서로 둠 |
| `/settings/sound/output/vs/0` | `deviceType`, `alarmInMute`. 쓰기 계약을 광고하는 형제 필드가 없어 읽기 전용 |
| `/statistic/pour/vs/0` | `last.capacity`의 단위 미확인 (레퍼런스도 단위 없이 노출) |

같은 릴리스의 `/settings/sound/{mode,volume}`는 **이식했습니다** — 이미 정의된 capability를
재사용하므로 새 정의·아이콘·플로우 카드가 필요 없습니다. 위 셋은 커스텀 capability를 새로
만들어야 하는데, 정수기는 실기기 검증이 없는 종류이고 세 값 모두 레퍼런스 스스로 의미를
확정하지 못한 진단값입니다. 정수기 실기기 덤프가 생기면 함께 확정합니다.

`supportedHotTemperatures`가 없는 보드에서 온수 온도 선택을 끄는 `#196`의 게이트는
**해당 사항이 없습니다** — 우리 정수기 레지스트리에 온수 온도 capability 자체가 없습니다.

### 냉장고 이산 설정값 — 양쪽 칸 (`#186`, `#229`)

`/temperature/definite/cooler/vs/0`와 `/temperature/definite/freezer/vs/0` —
`supportedList`가 연속 범위가 아닌 단문형 냉장고용입니다. 두 리소스는 필드명까지 같고
값만 부호가 다릅니다(냉동은 `['-23','-21','-19','-17','-15']` 같은 음수 목록).
`x.com.samsung.da.definite.desired`를 쓰고, 선택지는 같은 리소스의
`x.com.samsung.da.definite.supportedList`에서 옵니다. 레퍼런스는 `#229`에서 냉동 쪽을
추가했습니다.

**보유 냉장고 3대에 두 리소스가 모두 없습니다.** 게다가 Homey는 enum의 값을 매니페스트에
정적으로 선언해야 하므로, 기기가 주는 목록을 읽을 덤프 없이 만들면 값 자체가 창작이
됩니다. 이 보드를 가진 분의 덤프가 필요합니다.

### 제습기 물통 조명의 색상·밝기 (`#271`, `#231`)

`/watertank/lighting/vs/0`의 `colorOption`·`mode`는 이식하지 않았습니다. 켜기/끄기와
`waterfullAlarmStatus`는 이식했습니다(값 영역이 확실한 쪽). 선택지가 기기의
`colorSupportedList`·`modeSupportedList`에서 오는데, 위와 같은 이유로 Homey에서는 정적
선언이 필요하고 우리에게는 읽을 덤프가 없습니다. **레퍼런스 덤프에 목록이 들어 있으면 그
값으로 선언할 수 있습니다** — 다음 동기화 때 확인할 일입니다.

### 에어컨 환기 알림 — `/ventilation/setting/vs/0` (`#270`)

이식하지 않았습니다. **레퍼런스 스스로 확인되지 않은 추측이라고 적어 둔 항목**입니다:
`alarm` 필드만 있고 값 집합을 확인할 `supportedModes`가 없어서, 다른 리소스의 On/Off
관례를 근거로 스위치로 모델링했다고 밝히고 있습니다. 그리고 **우리 에어컨 4대는 이 href를
보고하지 않습니다** — 추측을 검증할 방법이 없는 채로 들여올 이유가 없습니다.

### 오븐 스위치 2종 (`#183`)

`EnergySaving`, `BurnerOnAlert` 옵션 토큰. 오븐 실기기가 없습니다.

## 레퍼런스 동기화 2026-08-08 (`4e47a1c` → `9228da1`, 비머지 23커밋)

세 가지가 이번 검토의 결론입니다. **대부분은 우리가 앞서 있었습니다** — 8/4에 덤프를
훑어 고친 것들을 레퍼런스가 8/7에 따라왔습니다(`/edgelighting`·`/light/stateful`·
`/mds/absenceclean`·`/airlevelcheck` 바인딩, 인덕션 `/alarms/vs/0`, 미세먼지 Insights =
레퍼런스의 `state_class`, 오븐 `UpperLamp` 유령 스위치는 애초에 안 묶음). 타입 라우팅도
18종/27토큰 갭 0.

이식한 것: 엣지라이팅 `mode`·`colorOption`(실측 후 쓰기 가능), 쓰기 경로의 재접속 재시도,
폴 실패 시 push 상태 해제. 아래는 이식하지 않았거나 아직 남은 것입니다.

### PEM 정규화 (`#291`) — 이식하지 않았습니다 (2026-08-08 실측)

레퍼런스는 붙여넣은 PEM의 BOM·CRLF·빈 줄을 걷어내는 `_normalize_pem`을 추가했습니다.
작고 무해해 보여서 그대로 가져올 뻔했는데, **우리 스택의 두 파서 모두 그중 무엇도
거부하지 않습니다.**

| 파서 | BOM | CRLF | CR | 빈 줄 |
|---|---|---|---|---|
| `cryptography` 49.0.0 `load_pem_private_key` | OK | OK | OK | OK |
| pyOpenSSL `load_certificate`/`load_privatekey` | OK | OK | — | — |

사용자가 없는 방어 코드이고, 자격증명 경로에서 사용자가 붙여넣은 바이트를 그 사용 가능
여부를 판정하는 자리 **앞에서** 다시 쓰는 일은 공짜가 아닙니다. 인덕션 전원 토글과 같은
판단입니다. `tests/test_pasted_pem_normalization.py`가 재개 조건을 들고 있습니다 — 파서를
올렸을 때 그 테스트가 깨지면 그때 이식하면 됩니다.

### `/settings/sound/optimization/vs/0` — 양쪽 다 안 읽습니다

에어컨 4대 전부가 `{"status": "On", "supportedModes": ["On", "Off"]}`를 보고합니다.
레퍼런스에도 없습니다. `_read_flag`/`_write_flag`로 바로 붙는 모양이지만, **무엇을
최적화하는 설정인지가 확인되지 않았습니다.** 쓰기를 해보고 소리가 어떻게 달라지는지
확인하면 끝나는 일입니다.

### `welcomeLightStatus` — `/edgelighting/vs/0`

같은 리소스에 `On`으로 살아 있고 양쪽 다 안 읽습니다. 냉장고의
`localthings_welcome_light`(`/proximity/vs/0`)와 **이름만 같고 다른 것**이므로 capability를
새로 만들어야 합니다. 사람이 다가올 때 켜지는 동작으로 보이지만 확인 안 됨.

### 굿슬립 — CAC에는 `Comode_` 토큰이 없습니다

우리 에어컨은 `/mode/vs/0` options에 `Sleep_16`을 들고 있습니다(= 8시간). 레퍼런스가
`30bd0fd`에서 측정한 것은 **`Sleep_<n>` 단독 쓰기는 2.04를 받고 버려진다**는 것이고,
`Comode_Sleep`과 같이 보내야 유지됩니다. 그런데 그 측정은 ARTIK051_KRAC이고,
**우리 CAC 덤프에는 `Comode_` 토큰 자체가 없습니다.** 읽기만 붙이는 것은 가능하지만
쓰기 계약이 이 보드에서 미확인이라 보류합니다. 판정 실험: `Comode_Sleep`+`Sleep_4`를
보내고 +8초·+45초에 다시 읽기.

### 냉장고 공기필터 — `/filter/airdustfilter/vs/0` (`#318`)

레퍼런스가 TP1X_REF_21K에 추가했습니다. **우리 TP2X 3대에는 이 리소스가 없습니다.**
레퍼런스가 덤프(`refrigerator_tp1x_ref_21k_airfilter_device.json`)를 같이 넣었으므로
미검증 계열 커버리지로는 추가 가능하고 `test_no_dead_mappings`도 통과합니다.
`filterUsage`가 여기서는 이미 0-100 퍼센트라 `filterCapacity`로 나누면 안 됩니다.

### 제습기 물통 조명 — 막고 있던 조건이 풀렸습니다

위 "제습기 물통 조명의 색상·밝기" 항목이 "레퍼런스 덤프에 목록이 들어 있으면"을 조건으로
보류돼 있었는데, **들어 있습니다**(`dehumidifier_tp1x_dhm01001_device.json`):

    colorSupportedList: WarmWhite, NaturalWhite, CoolWhite, Green, Purple, Blue, Orange, Pink
    modeSupportedList:  High, Low

엣지라이팅에서 같은 모양(`colorOption`+`colorSupportedList`)을 이번에 처리했으므로 방법도
정해져 있습니다. 다만 제습기 실기기가 없으므로 쓰기는 미검증으로 남습니다.

### 레퍼런스에 보낼 정정 — `has_extend_option_code`의 근거

`registry/capabilities/airconditioner.py`의 docstring이 "모든 RAC 계열 덤프는
`ExtendOptionCode`를 갖고 FAC/CAC 계열은 옛 맵만 갖는다"고 적었습니다. **우리 CAC 덤프도,
레퍼런스 자신의 `airconditioner_cac_device.json`도 `ExtendOptionCode_0`을 갖고 있습니다.**
값이 0이라 비트가 전부 0으로 읽히므로 동작 결과는 의도대로지만, 근거로 적힌 전제는
사실과 다릅니다. 이 함수가 "이 보드가 그 비트 위치를 문서화한 계열인가"의 대리 지표로
쓰이고 있으므로 정정할 값이 있습니다.

## 레퍼런스 동기화 2026-08-10 (`9228da1` → `ef66d1d`, v0.19.0 → v0.21.0)

### 김치냉장고 — 붙기는 붙고, 다섯 개만 읽습니다 (실측)

레퍼런스가 김치냉장고를 제대로 지원하기 시작했습니다(`KIMCHI_ZONE`, `KIMCHI_DOOR_GENERIC`,
숙성 상태·남은 시간·선반 수, `/status/kimchi/<슬롯>/vs/0`). 덤프도 세 개 들어왔습니다.

**우리 레지스트리에 그 덤프를 통과시켜 봤습니다.** 24개 capability 중 읽히는 것:

| 덤프 | 읽음 |
|---|---|
| `refrigerator_tp2x_ref_20k_kimchi` | 5 / 24 |
| `refrigerator_tp1x_ref_21k_kimchi` | 6 / 24 |
| `refrigerator_winecellar` | 7 / 24 |
| `refrigerator_tp1x_ref_21k_autodoor` | 9 / 24 |

읽히는 다섯: `alarm_contact`, `localthings_firmware_update`, `measure_power`,
`meter_power`, `meter_power.consumption`. **온도도, 모드도, 숙성도 없습니다.** 한국
사용자가 김치냉장고를 붙이면 문과 전력만 보이는 기기가 생깁니다.

`test_no_dead_mappings`는 이것을 못 잡습니다. 그 검사의 두 번째 불변식은 "같은 타입의 모든
덤프에서 빈 spec"인데, 일반 냉장고 덤프가 그 spec들을 채우므로 김치 덤프에서만 비는 것은
통과합니다.

~~**막고 있는 것은 매핑이 아니라 구조입니다.**~~ **틀렸습니다 — 같은 날 구현하며 확인했습니다.**
레퍼런스가 `href_prefix="/status/kimchi/"`를 쓰는 것은 사실이지만, 그것은 Home Assistant가
엔티티를 동적으로 만들기 때문입니다. Homey는 어차피 칸마다 capability를 선언해야 하므로
**슬롯당 Spec 하나면 되고**, `Spec.applies`가 href 유무로 이미 걸러줍니다. 새 기구는 필요
없었습니다. 소스를 읽지 않고 레퍼런스의 구현 방식만 보고 규모를 판단한 것이 원인입니다.

**구현 완료 (1.0.7).** 두 배치 모두 지원합니다:

| 덤프 | 이전 | 이후 |
|---|---|---|
| `refrigerator_tp2x_ref_20k_kimchi` (3칸) | 5 | **17** |
| `refrigerator_tp1x_ref_21k_kimchi` (1도어) | 6 | **10** |

칸마다 보관 모드(쓰기)·숙성 상태·숙성 남은 시간·선반 수. 모드 29종의 한국어 라벨은
레퍼런스가 제보자의 SmartThings 화면에서 옮겨 적고 `supportMode` 순서로 교차 검증한 것을
그대로 씁니다 — 우리가 지어낸 이름이 아닙니다.

**남은 미확인 두 가지**:

- **모드 쓰기는 검증되지 않았습니다.** 실기기가 없고 레퍼런스도 자기 쓰기 경로를 미확인이라
  적어 뒀습니다. `appliances.py`의 규칙("쓰기는 레퍼런스의 판단을 따른다")에 따라 내보내되,
  칸이 광고하는 `supportMode`에 없는 값은 거부합니다.
  `test_kimchi_refrigerator.py::test_the_write_is_declared_unverified`가 이 사실을 고정하고
  있으니, 나중에 "검증됨"으로 바꾸려면 측정을 같이 가져와야 합니다.
- **`ripeRemaintime`의 단위를 모릅니다.** 덤프 4개 전부 `ripeStatus`가 `Off`라 타이머가 도는
  것을 본 적이 없습니다. 분과 시간 둘 다 그럴듯해서 이름을 붙이지 않고 맨 숫자로 뒀습니다.
  **판정 실험**: 숙성 모드를 걸고 `ripeRemaintime`과 `ripeTotaltime`을 두 번 읽어 감소폭을
  보면 끝납니다.

### OCF 타입 4종을 놓치고 있었고, 검사가 그것을 못 봤습니다

`_OIC_TYPE_TO_KEY`에 `oic.d.range`·`oic.d.krefrigerator`·`x.com.st.d.dehumidifier`·
`x.com.st.d.winecellar`가 없었습니다. **`check_reference_coverage.py`가 이 표를 아예
비교하지 않았기 때문에** 그동안 "no gaps"를 출력했습니다. 스크립트 자신의 독스트링이
경고하던 실패 방식입니다("실패할 수 없는 검사는 없는 것보다 나쁘다"). 표를 비교 대상에
넣었고, 항목 하나를 지워 exit=1이 나오는 것을 확인했습니다.

다만 **위 덤프들은 원래도 보드 토큰으로 냉장고에 붙고 있었습니다** — OCF 항목을 넣어도
라우팅 결과가 달라지지 않습니다. 값이 있는 곳은 보드 토큰을 못 읽는 기기입니다.
레퍼런스가 `oic.d.range`를 넣은 이유가 그것입니다(그 보드에는 `/information/vs/0`이 없어
토큰을 읽을 데가 없습니다).

### 이미 우리가 앞서 있던 것

- **`filterUsage` 이중 변환(#330)** — 레퍼런스가 `filterCapacity`로 또 나누고 있었습니다.
  `shared.py`의 `_filter_percent`가 그 나눗셈을 "이 함수의 원래 추측이었고 확인한 모든
  기기에서 틀렸다"고 적으며 이미 걷어낸 상태입니다.
- **희망온도 증분 스냅(#276)** — 레퍼런스는 정수로 반올림해 0.5도 설정을 버리고 있었고,
  `/temperatures/vs/0` 폴백은 `increment`가 `items[]` 안에 있어서 한 번도 동작하지
  않았습니다. 우리 `_write_setpoint`는 처음부터 `items[]`에서 읽습니다.
- 세탁 코스명 뒤바뀜(#343), 지연 시작 자릿수(#338), 세탁/건조 완료 유지 — 해당 없음
  (코스 표도, 지연 포맷도 우리에게 없습니다).

### 학습형 모드(#327) — 우리는 정적으로 처리합니다

레퍼런스는 기기가 보고하지만 `supportedModes`에 없는 모드를 학습해 저장합니다. 우리는 같은
문제를 목록에 넣어 두는 방식으로 처리해 왔고(`WIND_DIRECTIONS`의 `Fix`), 이번에
`CONVENIENT_MODES`도 덤프 21개를 세어 채웠습니다. 학습은 config entry 저장소와 옵션 플로우가
필요한 기구라 지금은 이식하지 않습니다. **다시 볼 조건**: 덤프에 없는 코드를 보고하는 기기
제보가 오는 것.

## 레퍼런스 동기화 2026-08-11 (`ef66d1d` → `ac995dc`, 비머지 24커밋)

새 기능은 하나입니다 — **세탁기의 클라우드 "다운로드 코스"** (`#342`). 그리고 우리에게는
이식할 수 없습니다. 이유가 세 개인데 순서가 중요합니다.

**1. 토대가 없습니다.** 이 기능은 `laundry.py`의 코스 선택(`/course/vs/0`) 위에 얹힙니다.
**우리는 코스를 아예 모델링하지 않았습니다** — 세탁기 capability 17개 중 코스/사이클 선택이
없습니다. 다운로드 코스만 따로 이식하는 것은 성립하지 않습니다.

**2. Homey의 enum 값은 매니페스트에 정적으로 선언됩니다.** 다운로드 코스는 **사용자가 이름을
붙이는** 항목입니다(가전이 이름을 보고하지 않아 레퍼런스도 옵션 플로우로 사용자에게 묻습니다).
정적 선언과 정면으로 부딪칩니다. 제습기 물통 조명에서 만난 것과 같은 벽인데, 그때는 덤프에
목록이 있어서 풀렸고 이번에는 목록 자체가 사람 머릿속에 있습니다.

**3. 나머지는 HA 배관입니다.** 변경 1,550줄 중 `cloudcourse.py`(377) + `config_flow.py`(411)
+ `coordinator.py`(229)가 영속 저장소·Repairs 이슈·옵션 플로우입니다. 레지스트리 쪽은
`laundry.py` 151줄뿐입니다.

### 그 대신 진짜 구멍은 코스 선택 자체입니다

레퍼런스의 라벨 표는 이미 있고 한국어까지 붙어 있습니다:

| 표 | 라벨 수 | 쓰는 기기 |
|---|---|---|
| `washer_cycle_table_02` | 71 | 세탁기 |
| `dryer_cycle_table_03` | 27 | 건조기 |
| `dishwasher_cycle` | 19 | 식기세척기 |

덤프 15개가 `/course/vs/0`을 보고합니다. **코스 코드는 표 사이에서 안 통합니다** — 세탁기와
건조기가 공유하는 코드 20개가 전부 다른 뜻입니다(`01`=표준세탁/표준건조, `1d`=쾌속세탁/타월).
우리는 세탁기·건조기·식기세척기가 각각 다른 레지스트리라 capability를 나누면 충돌하지 않고,
어느 표를 쓸지는 기기 자신의 `x.com.samsung.da.st.courseTable`이 정합니다. 김치냉장고에서 쓴
방식(덤프에서 값 집합을 세고, 기기가 광고하는 것으로 쓰기를 거름)이 그대로 적용됩니다.

**읽기는 구현했습니다 (같은 날).** 세탁 71 / 건조 27 / 식기 19개 이름, 덤프 15개 중 11개가
코스를 읽습니다(에어드레서 3대는 표가 없어 제외, FlexWash는 아래 참고).

구현하며 두 가지가 처음 생각과 달랐습니다.

- **`/course/vs/0`의 `Course_` 토큰이 아니라 `/st/washercourse/vs/0`의
  `washerMode`를 읽습니다.** 후자는 `Table_02_Course_1C`처럼 표와 코드를 한 필드에 담아
  스스로 설명합니다. **일체형 세탁건조기가 이것을 결정했습니다** — 이 기기는 세탁
  `Table_02_Course_01`과 건조 `Table_03_Course_01`을 **둘 다** 보고하는데,
  `/course/vs/0`에는 `Course_01` 하나뿐이라 어느 쪽인지 알 수 없습니다. 첫 구현이 실제로
  틀렸습니다 — 건조 코스를 세탁 표로 이름 붙였습니다. 두 출처는 둘 다 있는 덤프 10개에서
  전부 일치하므로 구체적인 쪽을 택해도 잃는 것이 없습니다.
- **영어 라벨로는 표가 다르다는 것을 증명할 수 없습니다.** 공유 코드 20개 중 19개가 영어로
  다르고 20개가 한국어로 다릅니다. `01`만 영어로는 양쪽 다 "Normal"이고 한국어로는
  표준세탁/표준건조입니다. 일체형에서 두 타일에 "Normal"이 나란히 뜬다는 뜻이라, 영어 카탈로그
  쪽 결함입니다.

**미번역 표는 비워 둡니다.** FlexWash가 `Table_00`을 보고하는데 그 표는 아무도 라벨을 만든
적이 없습니다. 코드 `A5`는 Table_02에도 있고 거기서는 다른 코스라, 세탁 카탈로그로 대신
읽으면 자신 있게 틀린 이름이 뜹니다. 표를 키에 넣은 이유가 그것이므로 빈 채로 둡니다.

### 코스 쓰기 — 넣지 않기로 했습니다 (종결, 2026-08-11)

읽기만 내보냅니다. 근거 셋인데, 첫 번째가 결정적입니다.

**기기가 실제로 받는 코스 목록을 절반은 알아낼 수 없습니다.** 쓰기를 거를 유일한 방법이 그
목록인데, 덤프 15개 중 6개는 `editCourseList`가 없고 `/course/vs/0`의 `supportedOptions`라는
불투명한 패킹 레코드에서 **레코드 폭을 추측해** 꺼내야 합니다. 레퍼런스의 두 가드(코드가
전부 서로 다를 것, 현재 코스를 포함할 것)를 그대로 걸고 6개에 돌려봤습니다:

| 덤프 | 유도된 코드 수 | 라벨 없는 코드 |
|---|---|---|
| `washer_wa55a7700av` | 12 | 0 |
| `washer_ww5000c_cloud` | 14 | 0 |
| `washer_wa8000t` | 13 | **9** |

`wa8000t`은 가드를 통과하는 폭이 11과 143 둘뿐이고 143은 코스 1개짜리 퇴화 값입니다. 즉
가드가 폭을 하나로 좁히지 못합니다. **라벨 없는 코드가 곧 유도 실패는 아닙니다** — 명시적
`editCourseList`를 가진 일체형도 21개 중 14개가 라벨이 없으니, 그건 카탈로그 구멍입니다.
그래도 폭이 하나로 안 좁혀지는 것은 남습니다.

나머지 둘: Homey 드롭다운은 정적이라 71개를 다 띄우는데 기기가 실제 갖는 것은 13~24개이고,
실기기가 없어 쓰기 계약 자체가 미검증입니다.

**세탁기는 코스를 잘못 걸면 세탁 한 번을 버립니다.** 인덕션 전원 토글(1.0.3)과 같은 판단을
합니다 — 근거가 이 정도면 안 내보냅니다.

**다시 열 조건**: (1) 세탁기·건조기·식기세척기 실기기 제보로 쓰기가 확인되거나,
(2) `editCourseList`를 보고하는 기기에 한정해 내보내는 것으로 범위를 좁히거나(그러면 목록
추측이 필요 없습니다), (3) `supportedOptions`의 레코드 폭을 확정할 근거가 나오는 것.
(2)가 가장 가깝습니다 — 덤프 기준 9/15가 해당합니다.

### 해당 없음

- `device_class='lock'` 제거(`#349`) — HA `SwitchDeviceClass`가 `outlet`/`switch`만 받아
  플랫폼 설정이 죽던 버그입니다. Homey에는 대응물이 없습니다.
- `13d2a38` UUID 파일전송 네임스페이스 프로빙 회피 — `subdevices.py` 안이고, 우리는 subdevice
  탐색을 하지 않습니다.
- 라우팅: 18종/27토큰/15 OCF 타입 갭 0.

## 레퍼런스 동기화 2026-08-13 (`ac995dc` → `cbe8818`, v0.21.1/v0.21.2)

### 지워진 알람이 캐시에 영원히 남던 버그 (`#348`) — 고쳤습니다

푸시로 온 rep을 캐시에 **얕게 병합**합니다. 일반적으로는 맞습니다 — 알림이 바뀐 필드만 담아
올 수 있으니 없는 키는 "그대로 유지"입니다. `/alarms/vs/0`에서만 그 추론이 거꾸로입니다.
**이 리소스의 빈 상태가 빈 rep이기 때문입니다.** 우리 덤프로 확인했습니다:

| 덤프 | `/alarms/vs/0` |
|---|---|
| 냉장고 3대 전부 | `{}` |
| 에어컨·후드 | `items[]` 배열 |

즉 냉장고는 이상이 없을 때 `{}`를 보고합니다. 그것을 병합하면 아무 일도 일어나지 않으므로,
한 번 뜬 알람이 앱이 도는 동안 캐시에 남습니다. 폴은 rep을 통째로 다시 만들어 지우지만
**푸시 중인 기기는 5분 요약 스윕만 돌므로** 평상시에는 아무것도 지우지 않습니다.

`_replaces_on_notify`가 이 href만 병합 대신 교체합니다. **"빈 rep이면 항상 교체"로
일반화하지 않았습니다** — 다른 리소스에서 빈 알림이 무슨 뜻인지는 측정된 바가 없고, 추측하면
킵얼라이브 하나로 타일을 비우게 됩니다. 리소스 하나, 측정 하나, 규칙 하나.

매칭은 `alarms` 세그먼트 전체로 합니다. 접미사 검사(`endswith("alarms/vs")`)는 똑같이 읽히면서
`/kimchialarms/vs/0` 같은 것까지 집어가는데, 그런 리소스는 어느 덤프에도 없으므로 **아무도
측정한 적 없는 리소스에서 캐시를 버리는** 오작동을 심어두는 셈입니다.

### 코스 표 Table_00 추가 (`#357`)

레퍼런스가 세탁 11 / 건조 11개 라벨을 추가했습니다. 제보자가 WF45R6300 세탁기와 DVE45R6300
건조기에서 **코스를 하나씩 걸어보고 원시 코드를 되읽어** 확인한 것입니다.

우리에게는 번역 추가가 아니라 **구조 변경**이었습니다. 세탁 Table_00과 Table_02는 같은 기기의
두 세대인데 코드 11개를 공유하고 그중 10개가 다른 뜻입니다(`70` = 강력세탁 vs 타월). Homey는
enum 값과 이름을 매니페스트에 선언하므로 **한 id가 두 표의 뜻을 담을 수 없습니다.** 그래서
capability를 표마다 하나씩 둡니다(`localthings_wash_cycle_t00` 추가). id에 표 접두사를 붙이는
방법도 있었지만 **1.0.8로 이미 배포된 값의 이름을 바꾸는 일**이라 택하지 않았습니다.

FlexWash는 여전히 비어 있고, 이유가 바뀌었습니다. 전에는 "그 표에 아무도 이름을 안 붙였다"
였는데 이제 Table_00에 이름이 있습니다. FlexWash가 보고하는 `a5`는 **세탁 카탈로그 어느
쪽에도 없고 건조 Table_00에만** 있습니다(이불). 표 id만으로 카탈로그를 합쳤다면 이 세탁기의
코스를 건조기 목록에서 "이불"로 이름 붙였을 것입니다.

### 이미 우리가 맞게 하고 있던 것

**`read_resource`의 Collection 본문(`#335`)** — 레퍼런스는 CBOR 본문이 Property map이 아니면
버려서 Collection이 `rep: {}`(= 리소스는 있고 내용이 없음)으로 읽혔고, 그 때문에 복합
에어컨의 실내기 열거 단서를 하나 놓칠 뻔했습니다. 우리 `Session.read_resource`는 처음부터
`{"value": decoded}`로 감싸 보존합니다.

### 해당 없음

- 세탁/건조 완료 유지(`#358`, 커밋 3개) — 완료 후 진행률을 100에 붙여두는 기능인데, 우리는
  그 기반(진행률 유지)을 이식하지 않았습니다. 레퍼런스도 경계를 잡느라 커밋 세 개를 썼고,
  실기기가 없어 검증할 방법이 없습니다.
- `13d2a38`·`11c71a6` subdevice/복합기기 프로빙 — 우리는 subdevice 탐색을 하지 않습니다.

## 레퍼런스 동기화 2026-08-17 (`cbe8818` → `d912bff`, v0.22.0 / v0.23.0)

### 우리가 잘못 붙여 배포한 라벨 하나 (`#376`)

세탁 Table_02의 코드 `06`을 **"XXL 세탁"**으로 내보냈는데, 상류가 **"이불"**로 정정했습니다.
원래 한국어가 이미 `이불`이었고 영어만 추측성 "XXL Laundry"였는데, 우리가 생성할 때 그
영어에 맞춰 한국어까지 바꿔 담았습니다. 1.0.8·1.0.9에 그대로 나갔습니다.

카탈로그도 함께 커졌습니다 — 세탁 Table_02 71 → **93**, 건조 Table_03 27 → **45**
(WF21T6500KV/DV19T8745BV 제보분과 `0A`/`B0` 추가).

### 실외온도 — 붙였습니다. 다만 상수는 확인 못 했습니다

`/mode/vs/0`의 `OutdoorTemp_<n>`에서 55를 빼면 섭씨입니다. 상류가 48시간 기상 피드 대조로
검증했고(r=0.92, 섭씨 보드 한정), 우리 네 대 전부 섭씨를 선언하며 84~87 → 29~32°C를 읽습니다.

**그런데 독립 검증에 실패했고, 감추지 않고 적어 둡니다.** 같은 시각 wttr.in의 서울 기온은
13°C였고 -55로는 설명이 안 됩니다(-72쯤이어야 합니다). 가전 쪽 손을 들어주는 근거 둘:

- 네 대가 **실내 28.5~30.5°C인 방을 실제로 냉방 중**이었습니다. 13°C인 날의 모습이 아닙니다.
- **r=0.92는 상관계수**라 상수 오프셋 오차는 그대로 통과합니다. 상류의 검증도 이 오차를
  잡아내지 못했을 수 있습니다.

그래도 내보내는 이유: 상수가 틀려도 곡선은 실제를 따라가고, 창밖과 비교하면 사용자가 바로
압니다. **판정 실험**: 하루 동안 `OutdoorTemp_` 원값과 신뢰할 수 있는 관측소 기온을 같은
시각으로 여러 번 기록해 차이의 평균을 봅니다. 그 값이 55가 아니면 상수를 고칩니다.

상류는 이 센서를 기본 비활성으로 냅니다 — 멀티스플릿이 실외기 하나의 값을 모든 실내기에
복제해 중복 센서가 생기기 때문입니다. **우리 하드웨어는 해당하지 않습니다**: 같은 순간에
87/85/84/85로 서로 다르게 읽으므로 하나의 값을 되풀이하는 구조가 아닙니다.

### 해당 없음 / 보류

- **PM 단위 코드포인트(`#365` 후속)** — HA의 `DEVICE_CLASS_UNITS`가 U+03BC만 받아서 U+00B5로
  쓰면 경고가 나던 문제입니다. Homey는 단위를 목록과 대조하지 않으므로 해당 없습니다.
  다만 같은 커밋이 확인한 **Dust=PM10 / FineDust=PM2.5 / SuperFineDust=PM1, µg/m³** 매핑은
  우리가 이미 그렇게 쓰고 있어 일치합니다.
- **청소기 스틱 배터리(`#369`)** — `/status/stick/vs/0`. **덤프가 없습니다**(제보자 실기기에서
  나온 것이고 픽스처로 커밋되지 않았습니다). 이식하면 `test_no_dead_mappings`의 첫 불변식에
  걸려 `ALLOWED_UNSEEN`에 넣어야 하는데, 그 목록은 "비워 둘 수 있으면 비워 두라"는 자리입니다.
  필드명을 확인할 방법이 없는 채로 넣는 것이 후드에서 겪은 실패 방식입니다. **다시 열 조건**:
  그 리소스를 담은 덤프.
- **smartthings-local 0.1.8 상향** — 우리가 실제로 설치하는 것은 `app.json`의
  `pythonPackages: smartthings-local>=0.1.2`이고, 배포 시점에 최신이 깔립니다. 즉 이미 새
  라이브러리를 쓰고 있고 상한을 올려도 설치 결과는 같습니다. (`python_packages/`의 0.1.1은
  gitignore된 로컬 캐시라 배포물이 아닙니다 — 로컬 실행이 프로덕션보다 낡은 라이브러리로
  돈다는 뜻이므로 알아둘 값어치는 있습니다.)
- 오프라인 스냅샷 설정(`#295`), 클라우드 코스 전역 토글, HA 통계 마이그레이션·enum 정규화 —
  전부 HA 배관입니다.
